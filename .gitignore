node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Relay server runtime artifacts & build output
server/data/
server/dist/
server/*.log

# Agent tooling — environment-specific, not project source
AGENTS.md
CLAUDE.md
/.opencode/
/.opencode-skills/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
